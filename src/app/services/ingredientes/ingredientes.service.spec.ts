import { IngredientesService } from './ingredientes.service';

describe('IngredientesService', () => {
  it('should create an instance', () => {
    expect(new IngredientesService()).toBeTruthy();
  });
});
